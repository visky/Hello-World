pipeline {
    node {
        checkout scm 
        echo 'Tamas tries the pipeline'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Build step'
            }
        }
    }
}